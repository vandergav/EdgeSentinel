{
  "name": "edgeone-copilot-runtime",
  "private": true,
  "version": "0.1.0",
  "description": "Thin AG-UI <-> CopilotKit protocol bridge. Zero agent logic — backend/edgeone_agents is the single source of truth for all agent behavior. This process only translates CopilotKit's frontend runtime protocol into an AG-UI HttpAgent call against the FastAPI backend.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@ag-ui/client": "0.0.57",
    "@copilotkit/runtime": "^1.50.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "rxjs": "7.8.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.3"
  }
}
